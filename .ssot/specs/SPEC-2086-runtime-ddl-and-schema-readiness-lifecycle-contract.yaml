schema_version: "0.8.0"
kind: "spec"
id: "spc:2086"
number: 2086
slug: "runtime-ddl-and-schema-readiness-lifecycle-contract"
title: "Runtime DDL and schema readiness lifecycle contract"
status: "accepted"
origin: "repo-local"
decision_date: null
tags: []
summary: "Declare the runtime-wide DDL and schema-readiness lifecycle contract for Tigrbl's Python runtime."
supersedes: []
superseded_by: []
status_notes:
  -
    at: "2026-04-24T14:18:27Z"
    note: "Accepted runtime-wide DDL/schema readiness lifecycle contract for Python and optional runtime binding."
    status: "accepted"
  -
    status: "accepted"
    note: "Updated accepted DDL/schema readiness contract for Python-only runtime scope."
    at: "2026-06-17T10:24:07Z"
references: []
body: |-
  ## Purpose
  
  Declare the runtime-wide DDL and schema-readiness lifecycle contract for Tigrbl's Python runtime.
  
  ## Scope
  
  This SPEC applies to Python executors, runtime engine adapters, SQLite and future durable engine implementations, and ordinary request execution for canonical operations.
  
  Explicit DDL/admin operations, migrations, bootstrap commands, and runtime/application initialization phases are in scope as allowed schema-readiness phases. Ordinary CRUD request execution is in scope as a prohibited DDL phase.
  
  ## Requirements
  
  1. DDL and schema-readiness work MUST run during runtime initialization, engine initialization, explicit bootstrap, explicit migration, or explicit DDL/admin operations.
  2. Ordinary Python request execution MUST NOT run table creation, index creation, schema migration, schema reflection, or implicit schema-readiness repair.
  3. Idempotent DDL such as `CREATE TABLE IF NOT EXISTS` and `CREATE INDEX IF NOT EXISTS` is still DDL and MUST NOT run on ordinary request hot paths.
  4. If schema readiness has not been established, ordinary request execution MUST fail closed with a clear configuration or runtime error.
  5. A deliberately configured development auto-initialization mode MAY run schema-readiness work only when that mode is explicit and evidenced as outside production request semantics.
  6. Optional Rust/runtime-binding execution is retired under `adr:1185` and is not a required conformance lane.
  7. Engine package certification for schema readiness MUST prove `app.initialize()` or another governed initialization entrypoint creates usable schema before ordinary canonical operation execution.
  
  ## Python Runtime Requirements
  
  1. Python request execution MUST NOT call `create_all`, migration routines, implicit table bootstrap, or schema readiness helpers for ordinary CRUD requests.
  2. Python initialization, bootstrapping, and explicit DDL/admin operations MAY perform schema readiness when explicitly requested.
  3. Python evidence MUST prove ordinary request execution does not perform DDL or implicit schema readiness.
  4. DuckDB engine evidence MUST prove `TigrblApp` initialization creates DuckDB-backed tables and subsequent sessions can operate on those tables without request-path DDL repair.
  
  ## Evidence Requirements
  
  - Unit or integration tests showing DDL happens during initialization or explicit readiness phases.
  - Request-path tests or instrumentation proving DDL does not run for ordinary CRUD requests.
  - Fail-closed tests for missing schema readiness.
  - Engine-package tests linking schema readiness to the package's advertised provider and session capabilities.
  - Rust/runtime-binding evidence is not required and must not be used as a core release gate.
  
  ## Non-Goals
  
  - This SPEC does not define a migration language.
  - This SPEC does not require a Rust-native server or a repo-wide Rust certification lane.
  - This SPEC does not prohibit explicit DDL/admin operations.
spec_kind: "normative"
adr_ids:
  - "adr:1083"
  - "adr:1084"
  - "adr:1185"
