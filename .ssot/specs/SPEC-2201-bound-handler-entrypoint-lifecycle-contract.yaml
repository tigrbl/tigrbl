schema_version: "0.8.0"
kind: "spec"
id: "spc:2201"
number: 2201
slug: "bound-handler-entrypoint-lifecycle-contract"
title: "Bound handler entrypoint lifecycle contract"
status: "accepted"
origin: "repo-local"
decision_date: null
tags: []
summary: "Contract: model mapping binds handlers.<op>.invoke, handlers.<op>.dispatch, and handlers.<op>.core to the parent class. invoke executes the full op lifecycle, dispatch skips ingress, post-commit, and egress while retaining dispatch-side behavior, and core invokes only the parent-bound op core callable. Existing raw compatibility aliases remain callable but are not the canonical names."
supersedes: []
superseded_by: []
status_notes:
  -
    status: "accepted"
    note: "Accepted with runtime implementation and focused tests on 2026-06-24."
    at: "2026-06-24T22:17:48Z"
references: []
body: |-
  Contract: model mapping binds handlers.<op>.invoke, handlers.<op>.dispatch, and handlers.<op>.core to the parent class. invoke executes the full op lifecycle, dispatch skips ingress, post-commit, and egress while retaining dispatch-side behavior, and core invokes only the parent-bound op core callable. Existing raw compatibility aliases remain callable but are not the canonical names.
spec_kind: "operational"
adr_ids:
  - "adr:1197"
