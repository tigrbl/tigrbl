# Planned Test: WebTransport Tigrcorn Mixed Lane Framing Integration

This planned test will verify that stream and datagram framing metadata remains lane-local across mixed WebTransport traffic under one Tigrcorn-backed Tigrbl session.
