# Tigrcorn WebTransport Single Session Demo

Planned Tigrcorn server-client proof for one WebTransport session carrying:

- one bidirectional stream
- one unidirectional stream
- datagram traffic

This is intentionally narrower than the broader session multiplexing contract.
