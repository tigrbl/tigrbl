# Transport Compatibility Imports

Planned unit coverage for deprecated transport compatibility import stability.

The eventual executable test must assert public compatibility imports continue
to resolve during the deprecation window while replacement APIs are documented.
