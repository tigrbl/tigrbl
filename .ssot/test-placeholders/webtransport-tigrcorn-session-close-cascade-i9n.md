# Planned Test: WebTransport Tigrcorn Session Close Cascade Integration

This planned test will verify that WebTransport session close terminates active stream ownership and prevents later stream or datagram sends in the Tigrcorn bridge path.
