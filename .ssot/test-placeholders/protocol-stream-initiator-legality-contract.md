# Protocol Stream Initiator Legality Contract

Planned unit coverage for the protocol stream support matrix.

The eventual executable test must prove valid h11 request-body and response-body
streaming, valid h2 client-initiated request stream behavior, valid plain h3
client-initiated request stream behavior, and fail-closed rejection for plain h3
server-initiated bidirectional streams unless an extension binding authorizes
them.
