# Planned Test: WebTransport Tigrcorn Message Lane Rejection Integration

This planned test will verify that native `webtransport.message.*` events are rejected or absent in the Tigrcorn bridge path.
