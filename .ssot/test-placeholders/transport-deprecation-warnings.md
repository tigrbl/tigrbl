# Transport Compatibility Deprecation Warnings

Planned unit coverage for deprecated transport compatibility module warnings.

The eventual executable test must assert the exact DeprecationWarning text for
REST, JSON-RPC model, and JSON-RPC helper compatibility imports across both
`tigrbl.transport` and `tigrbl_concrete.transport` namespaces.
