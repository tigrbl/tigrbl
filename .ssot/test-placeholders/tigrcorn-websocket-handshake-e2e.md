# Tigrcorn WebSocket Handshake E2E

Planned Tigrcorn server-client proof for `ws` and `wss` opening handshake behavior.

This row is intentionally a planned placeholder until the runtime accepts the
websocket connection before waiting for the first client payload.
