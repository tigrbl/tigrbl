# Python-only runtime: SSOT and docs Rust parity ban

Planned governance scan for Rust parity retirement.

Assertions:
- Active SSOT ADRs, SPECs, features, and tests do not require optional Rust/runtime-binding parity except retirement records.
- Developer docs do not advertise optional Rust runtime binding support as current.
- Rust runtime demos and Rust benchmark lanes are removed from required core verification paths.
