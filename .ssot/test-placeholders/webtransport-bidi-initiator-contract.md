# WebTransport Bidi Initiator Contract

Planned runtime coverage for WebTransport bidirectional stream initiator
metadata.

The eventual executable test must prove client-initiated and server-initiated
bidirectional streams are distinguishable in lane metadata while still sharing
the same lane-local inner framing legality rules.
