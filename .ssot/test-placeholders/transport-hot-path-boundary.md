# Transport Hot Path Boundary

Planned architecture coverage for the transport compatibility boundary.

The eventual executable test must prove REST and JSON-RPC runtime hot paths do
not import deprecated `tigrbl.transport` or `tigrbl_concrete.transport` modules.
