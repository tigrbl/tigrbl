# JSON-RPC Codec Authority

Planned unit coverage for JSON-RPC runtime codec authority.

The eventual executable test must prove runtime JSON-RPC payload validation uses
`tigrbl_atoms.atoms.framing` codecs and not deprecated transport helper models.
