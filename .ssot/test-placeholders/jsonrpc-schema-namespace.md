# JSON-RPC Schema Namespace

Planned unit coverage for the non-transport JSON-RPC envelope schema namespace.

The eventual executable test must assert request, notification, success
response, error response, batch, id, and error object schemas are importable
from the replacement schema namespace.
