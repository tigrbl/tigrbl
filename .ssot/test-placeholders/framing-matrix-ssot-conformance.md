# Framing Matrix SSOT Conformance

Planned conformance coverage for SSOT framing matrix rows.

The eventual executable test must compare the SSOT support matrix, BindingSpec
policy, WebTransport lane policy, and runtime codec registry so support claims
cannot drift from executable coverage.
