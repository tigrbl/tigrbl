# tigrbl-runtime-bindings-rs

Optional Rust extension package for `tigrbl-runtime`.

Install it directly:

```bash
pip install tigrbl-runtime-bindings-rs
```

Or through the runtime extra:

```bash
pip install "tigrbl-runtime[rust]"
```
