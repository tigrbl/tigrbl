# MOQT LOC MSF WebTransport demo extraction

The `moqt_loc_msf_webtransport` authoring demo and its executable proof were
removed from this monorepo after the example was extracted to the standalone
`tigrbl/moqt-loc-msf-webtransport` repository.

The former monorepo test and evidence rows are retained only as archived SSOT
references. They no longer constitute executable or current local evidence in
this repository.
