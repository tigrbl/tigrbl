# ADR Pointer

Canonical ADR documents now live under `.ssot/adr/`.

- Next-target ADRs migrated from this directory are registered in `.ssot/registry.json`.
- Use `.ssot/adr/` for the canonical files.
