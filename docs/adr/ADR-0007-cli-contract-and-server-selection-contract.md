# CLI contract and server-selection contract

Accepted as a target contract; implementation pending.

## Decision

The unified `tigrbl` CLI is part of the current target surface. It must expose run/serve/dev/routes/openapi/openrpc/doctor/capabilities and explicit server selection across supported servers.
