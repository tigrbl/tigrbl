# Next Steps

## Immediate next phase

Phase 1 should freeze the exact current-target claim set.

## Priority work

1. populate and harden the claim registry across the current-target rows
2. complete the ADR set for boundary, claim tiers, release policy, docs authority, CLI contract, and deferred next-target work
3. add CI checks for package layout, docs pointers, root clutter, and claim wording
4. produce an authoritative implementation map for each partial and missing current-target row
5. decide which missing docs UI and operator surfaces remain in current-target scope versus being explicitly de-scoped
6. close the unified CLI contract or explicitly move it out of the current boundary

## Implementation closure themes after Phase 0

- OAS / JSON Schema / JSON-RPC / OpenRPC closure
- RFC / OIDC / auth behavior closure
- docs UI closure
- operator surface closure
- CLI closure
- reproducible evidence bundles and promotion gates
