import pytest


@pytest.mark.skip(reason="Planned SSOT coverage for docs projection of websocket transport metadata.")
def test_docs_websocket_transport_projection_contract():
    raise AssertionError("planned")
